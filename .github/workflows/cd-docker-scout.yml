name: Crear issue si hay vulnerabilidades críticas
  uses: actions/github-script@v6
  with:
    script: |
      const fs = require('fs');
      let outputRaw;
      try {
        outputRaw = fs.readFileSync('scout_output.json', 'utf8').trim();
      } catch (e) {
        core.setFailed("No se pudo leer scout_output.json");
        return;
      }

      // Validar si es JSON válido
      let output;
      try {
        output = JSON.parse(outputRaw);
      } catch (e) {
        console.log("No es JSON válido, salida capturada:");
        console.log(outputRaw);
        // Para evitar fallar el pipeline solo por JSON inválido, podemos salir aquí
        return;
      }

      if (!output.vulnerabilities) {
        console.log("No se encontraron vulnerabilidades o formato inesperado.");
        return;
      }

      const criticalVulns = output.vulnerabilities.filter(v => v.severity === 'critical');

      if (criticalVulns.length > 0) {
        const issueTitle = `🚨 Vulnerabilidades críticas detectadas en la imagen Docker`;
        const issueBody = `Se detectaron **${criticalVulns.length}** vulnerabilidades críticas en la imagen \`mjtaehyung/proyecto:latest\`.\n\n` +
                          `Detalles:\n` +
                          criticalVulns.map(v => `- **${v.id}**: ${v.title} (Package: ${v.packageName})`).join('\n') +
                          `\n\nPor favor, revisa y corrige estas vulnerabilidades antes de continuar con el despliegue.`;

        // Verificar issues existentes para no duplicar
        const issues = await github.rest.issues.listForRepo({
          owner: context.repo.owner,
          repo: context.repo.repo,
          state: 'open',
        });

        const existing = issues.data.find(issue => issue.title === issueTitle);
        if (!existing) {
          await github.rest.issues.create({
            owner: context.repo.owner,
            repo: context.repo.repo,
            title: issueTitle,
            body: issueBody,
          });
          console.log("Issue creado por vulnerabilidades críticas.");
        } else {
          console.log("Ya existe un issue abierto con vulnerabilidades críticas.");
        }

        throw new Error("Se encontraron vulnerabilidades críticas. Se ha creado un issue.");
      } else {
        console.log("No se encontraron vulnerabilidades críticas.");
      }